[info]
version = "1.0.0"
omeka_version_constraint = "^4.0.0"
name = "IA Outbound"
description = "Push Omeka S metadata to Internet Archive, publish Collecting submissions, and push Contribute metadata revisions. Requires an IA collection and S3 API keys. Works standalone; pair with IA Inbound for import."
author = "c-host"
configurable = true
